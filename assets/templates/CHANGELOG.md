# Changelog: {{PROGRAM}}

Written by {{INTEGRATOR}} at every checkpoint, from the merges and the stage summaries, never from
memory. One section per checkpoint, newest at the top. Each names the `main` SHA the checkpoint
closed at, the lane branches merged with their heads, the shared claims and manifests applied,
every stage with its executed count and log path, the canonical states moved, and what was sent
back to a lane. Lanes do not write here; their record is the board and their handoffs.

## CP0 — {{DATE}}

- `main` at: (SHA)
- Installed: roadmap, registry, board, roster, coordination contract, decisions log, checkpoint
  protocol, lane briefs, validator.
- Stages: `gates` — (result, log).
- Known red on `main`: (attributed findings, or "none").
- Owner actions requested: (or "none").
