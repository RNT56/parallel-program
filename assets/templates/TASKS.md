# Task ledger: {{PROGRAM}}

The task IDs a lane may carry. One heading per task, in the exact shape the registry's
`taskSources` pattern expects. State lives here only if no other registry is canonical for it; the
board records progress, and the integrator moves a state on checkpoint evidence only.

### EX-001 — Example task title

Lane: L1-example. State: READY. Acceptance: one sentence a checkpoint can verify.
Dependencies: none.
